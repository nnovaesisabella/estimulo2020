import 'package:flutter/material.dart';
import 'componetes/body.dart';

class SplashHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blueAccent[700],
      //Página SplasHome body
      body: Body());
  }
}

